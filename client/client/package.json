{
	"name": "client",
	"version": "1.0.0",
	"description": "Client side code for RPG",
	"main": "src/index.js",
	"scripts": {
		"build": "npx webpack --mode production",
		"start": "npm run build && npx webpack serve --port=8000 --mode development"
	},
	"author": "Brady Robles",
	"license": "ISC",
	"devDependencies": {
		"raw-loader": "^4.0.2",
		"webpack": "^4.44.2",
		"webpack-cli": "^4.2.0",
		"webpack-dev-server": "^3.11.0"
	},
	"dependencies": {
		"phaser": "^3.24.1"
	}
}
